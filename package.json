{
  "name": "ecommerce-mern",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build":"npm install  && cd client && npm install --prefix && npm run build --prefix && cd ../server && npm install ",
    "start": "node server/server.js"
  },
  "author": "",
  "license": "ISC"
}
