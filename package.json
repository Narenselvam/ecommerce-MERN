{
  "name": "ecommerce-mern",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build":"npm install && cd client && npm install && npm run build && cd ../server && npm install && npm run dev"
  },
  "author": "",
  "license": "ISC"
}
